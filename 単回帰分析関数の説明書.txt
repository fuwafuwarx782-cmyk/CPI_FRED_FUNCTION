S3から二つのファイル、それぞれ2列のデータを取り出す。
pdモジュールのmargeメソッドで同じ名前の共通する列を頼りにデータを結合。
今回の場合は、日付の列を目印に実質GDP成長率の列とGDPギャップの列を結合した。
目印となる結合キーはon　howはデータの欠落への対応を決める
.dropna()はmergeメソッドでデータ欠落への対応を実行する。()はデフォルト
df_margedは表形式のデータフレーム型（pandasの形式）

目的変数と説明変数、切片を定義する。このデータを使ってデータ型が数値と伝える。

smモジュール（statismodelsモジュール）
OLSは最小二分法の英語　定義するメソッド
.fit()が最小二分法を実行するメソッド

あとは回帰式と相関係数をS3に保存するだけ
実はかなり単純な関数